"Use strict"; // treat all JS code as newer version

// alert(3+5)  We are using node js not browser
console.log(3+2);

let name = "Nivesh Soni"
let age = 18
let isLoggedIn= false
let pi = 3.14
let symbol = '&'

console.table(typeof name)
console.table(typeof age)
console.table(typeof isLoggedIn)
console.table(typeof pi)
console.table(typeof symbol)